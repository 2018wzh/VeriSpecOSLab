# 助教检查表

- [ ] `vos doctor` 的错误给出可操作修复。
- [ ] `vos spec check` 无 schema、引用或 owns 错误。
- [ ] 每个 target 都有 `verifies` 稳定 Spec ID。
- [ ] read-only Agent 角色没有源码或 Spec diff。
- [ ] 实现提交包含 Run-ID、Spec-Hash，且没有越过 owns。
- [ ] 报告、审计链和提交归档不含凭据或本机绝对路径。
