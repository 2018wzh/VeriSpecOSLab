# 第 1 章：操作系统初步 — 定义你要构建什么

## 1.1 为什么先设计再写代码

"我还没写一行代码，先花一整天写文档？"

是的。这一天是你整个课程中投资回报率最高的一天。

理由不在于"文档很重要"这个老生常谈。真正的原因更具体：操作系统的各个子系统是深度耦合的。你在阶段 3 选择的分页模型会影响阶段 5 的 trap 路径设计。你在阶段 1 写的资源模型路线会影响阶段 7 的 syscall ABI 和用户库。一个月后你发现阶段 5 和阶段 3 的设计打架，回头改——成本是现在的十倍。

ArchitectureSeed 的作用不是"交作业"。它是你未来三个月的设计锚点。每次你发现自己在两个可行方案之间纠结的时候，回到 ArchitectureSeed 看你的 goals 和 constraints——答案通常就在那里。

---

## 1.3 操作系统简史与设计哲学

### 1.3.1 Unix 哲学：简单、组合、一切皆文件

Unix 不是被"设计"出来的——它是在 PDP-7 上为了玩 Space Travel 游戏而逐渐长出来的。但它的哲学异常清晰，影响了此后半个世纪的操作系统设计。

核心原则：

**"做一件事，把它做好"（Do one thing and do it well）。** 每个程序只做一件事。`ls` 列出文件。`grep` 搜索文本。`wc` 计数字。它们不内嵌彼此的功能——通过管道（`|`）组合。`ls | grep foo | wc -l`：这三个程序各自都不知道对方的存在，但它们通过文本流协作，完成了一件复杂的事。

**"一切皆文件"（Everything is a file）。** 文件、设备、管道、网络连接——都用文件描述符（file descriptor）表示。`read(fd, buf, n)` 不关心 fd 指向的是一个磁盘文件还是一个串口还是一个管道。统一接口等于统一工具链，等于无限组合。

**"文本流作为通用接口"（Text streams as universal interface）。** 程序之间通过文本行通信。文本是人类可读的、可调试的、不绑定特定二进制格式的。这个决定在 JSON/YAML 流行的今天看起来理所当然，但在 1970 年代——当大多数系统使用专有二进制协议时——是革命性的。

Unix 哲学的代价是什么？统一接口是强大的，但当某种资源不适合"文件"的比喻时，你会感到别扭。网络套接字就是一个迟到的例子——BSD 花了很大力气才把 socket 塞进 fd 框架。Linux 的 `/proc` 文件系统把进程信息暴露成文件，读起来很方便，但写入时复杂的语义暴露了"一切皆文件"的边界。

### 1.3.2 微内核论战：Tanenbaum vs Torvalds, 1992

1992 年 1 月 29 日，Andrew Tanenbaum 在 comp.os.minix 新闻组发了一篇帖子，标题是 **"Linux is obsolete"**。

他的论点：Linux 的宏内核架构属于 1970 年代。在 1990 年代，微内核才是未来。微内核把文件系统、设备驱动、网络协议栈都移出内核，作为用户态服务进程运行。内核只保留最少的职责——地址空间、线程调度、IPC。一个服务崩溃了，内核不受影响。这是更好的模块化和容错性。

Linus Torvalds 的回复直击要害：微内核的理论优势在实践中被 IPC 开销抵消。一个文件系统在用户态运行，每次 `read()` 都需要多次跨进程消息传递——内核→文件系统服务→磁盘驱动服务→内核→用户。在宏内核中，这些全是函数调用。IPC 开销在 benchmark 中是实打实的，而"某个服务崩溃后可以重启"的优势在桌面和服务器场景中很少真的用到。

谁赢了？看市场份额，Linux 的宏内核赢了——运行在数十亿设备上。看安全性，seL4 的微内核赢了——它的内核被完整形式化验证，没有任何 bug（在形式化规范的意义上）。QNX（微内核）在汽车和工业控制系统中广泛使用，因为"某个服务崩溃不会拖垮整个系统"在这些场景中是真的卖点。

**这场论战对你这门课的意义**：没有"最好"的内核架构。宏内核快但模块间隔离弱。微内核安全但 IPC 有开销。你的选择取决于你的 ArchitectureSeed 中的 goals——是教学清晰性？是性能？是安全性？你的选择必须是你目标驱动的，不是跟风驱动的。

### 1.3.3 操作系统设计的"艺术"：权衡

操作系统的设计不是寻找最优解——最优解不存在。它是做权衡，并且诚实地说清楚你在什么维度上做了牺牲来换取什么。

几个经典权衡：

**性能 vs 隔离。** 宏内核把模块间交互做成函数调用——快，但一个模块的 bug 可能 corrupt 另一个模块的数据。微内核用 IPC 做模块间交互——慢，但模块崩了内核不受影响。

**兼容 vs 简洁。** 兼容已有 ABI（如 POSIX syscall）能让你运行大量现有程序，但那个 ABI 背负了几十年的历史包袱——`fork` 的语义、`dup2` 的微妙行为、`errno` 的混乱错误码。设计自己的干净 ABI 简洁优雅，但你的 OS 上没有任何现有程序能跑。

**通用 vs 专用。** 一个通用 OS 要对各种 workload 都"不算差"。一个专用 OS（如 unikernel）可以对一个 workload 做到极致，但换了 workload 就废了。

你在 ArchitectureSeed 中不需要解决所有这些权衡。但你需要意识到它们的存在。你的 non_goals——那些你明确排除的东西——本质上就是你声明"我选择了不优化这个维度"。

## 1.4 你要回答的六个设计问题

### 问题一：你的 OS 的职责边界在哪？

每个 OS 都在三个维度上做文章。

**资源抽象与复用。** CPU 只有一个（或几个），跑的程序有几十个。你怎么让每个程序都"觉得"自己独占 CPU？基本思路是快速切换——一个程序跑几毫秒，然后切换到下一个。切换太快了，人类感知不到。内存也是——物理内存是一块连续的 RAM，但每个程序有自己的"虚拟地址空间"，看到的是连续的、私有的内存。

**隔离与保护。** 资源复用产生了新问题：程序 A 怎么保证程序 B 不会偷看它的内存？答案是硬件机制——MMU（内存管理单元）把每个程序的"虚拟地址"翻译成"物理地址"的过程中，检查权限。程序 A 的虚拟地址 X 映射到物理地址 Y，程序 B 的虚拟地址 X 映射到物理地址 Z——各自以为自己在同一个地址，实际上被硬件强制分离。

**服务与接口。** 用户程序怎么请求内核做事？它不能直接操作硬件——那会绕过所有保护机制。所以有一个 syscall 接口——用户程序通过一条特殊指令（RISC-V 的 `ecall`）"叫"内核，内核在更高特权级执行请求，然后返回结果。这个接口的设计决定了你的 OS 的"性格"——它暴露了什么抽象？隐藏了什么细节？

你的 ArchitectureSeed 不需要在"资源复用"上标新立异——快速切换这个思路是共享的。你的设计选择主要落在"隔离的强度"和"接口的形态"上。

### 问题二：你选择什么内核架构？

这里有五种经典形态。按代码复杂度从低到高排序。

**宏内核（Monolithic Kernel）。** 所有内核服务——内存管理、进程调度、文件系统、设备驱动——跑在同一个特权级（S-mode）的同一个地址空间中。Linux、xv6、BSD 都走这条路。

写一个宏内核的时候，`kalloc()`（内存分配）、`bread()`（读磁盘块）、`fork()`（创建进程）——这些函数之间直接调用，不需要序列化/反序列化消息，不需要上下文切换。这是宏内核的最大优势：**性能**——模块间交互的成本只是一次函数调用。

代价是**隔离**。文件系统模块中的一个数组越界可能 corrupt 内存管理模块的数据结构。宏内核中所有模块"互信"——因为它们运行在同一个地址空间。一个模块的 bug 可以摧毁整个内核。大量内核漏洞的根因就是这里。

**微内核（Microkernel）。** 内核只提供最小机制——地址空间、线程、IPC。文件系统、设备驱动、网络栈都作为独立的用户态进程运行。seL4、Minix、QNX。

写一个基于微内核的系统时，用户程序调用 `read()` 的路径是：用户程序 → IPC 发送给文件系统服务 → 文件系统服务通过 IPC 请求磁盘驱动服务 → 磁盘驱动服务通过 IPC 请求内核访问 MMIO → 层层返回。每一步 IPC 都有上下文切换和消息序列化的开销。微内核的代价就是**性能**——"读取一个字节"可能涉及三次 IPC。

但它的收益也是实打实的：**隔离和容错**。文件系统服务崩了，内核不受影响——重启文件系统服务就行。驱动崩了，不影响其他服务。seL4 甚至通过形式化验证证明了它的内核代码在形式化模型下完全没有 bug——这在宏内核中几乎不可能做到（代码量太大）。

**混合内核（Hybrid Kernel）。** 核心服务（调度、内存管理）在内核态，其他服务（文件系统、驱动）可以在内核态也可以在用户态。Windows NT 和 macOS 的 XNU 走这条路。理论上取两者之长，实践中继承了宏内核的复杂度和微内核的 IPC 开销。

**Unikernel。** 应用代码和内核代码编译成同一个镜像，跑在同一个特权级。没有"用户态 vs 内核态"的边界，没有 syscall 开销。适合云上的单一用途虚拟机——那个虚机只跑一个应用，不需要多进程隔离。

**Exokernel。** 内核只做资源复用和保护（确保进程 A 不能访问进程 B 的磁盘块），但**不做抽象**。文件系统这个抽象由用户态库实现。应用可以选择最适合自己的"文件系统库"，而不是被迫使用内核提供的通用文件系统。MIT 的 Exokernel 证明了这种设计的可行性，但它的复杂度在用户态库那端——每个应用都要重新发明轮子。

**教学场景的推荐。** 如果你是第一次写 OS，宏内核是风险最低的选择。一来看参考资料最多（xv6、Linux 早期版本），二来调试方便（所有代码在一个地址空间）。如果你想挑战，微内核 + capability IPC 是一个很有深度但不轻松的方向。

### 问题三：你的 OS 为什么而存在？

**教学目的。** 你的 OS 存在是为了让你学会系统设计的原理。这意味着"代码清晰"比"跑得快"优先级高。你可能会做一个效率不高的分配器、一个简单的调度器——只要它们的设计理由在你的 ADR 中说清楚了，这就够了。

**兼容目的。** 你的 OS 运行已有程序。这意味着你被锁入了那个已有系统的 ABI——它的 syscall 编号、它的可执行格式、它的错误码语义。有好处：你的 OS 一诞生就有一个软件生态。也有代价：你继承了那个 ABI 的历史包袱。

**安全目的。** 你的 OS 提供可论证的隔离或安全保证。你可能需要 capability 系统、形式化验证的子集、信息流控制。这条路需要的理论基础最多，但产出也最具说服力。

**性能目的。** 在某个指标上做到接近或超过参考系统。你需要细致的 benchmark、系统性优化、可能牺牲代码清晰度。

这些目标可以共存但要排优先级。阶段 5 你发现一个"教学清晰"和"兼容 POSIX"冲突的设计选择——怎么办？如果你在 ArchitectureSeed 中写了"教学目标优先于兼容目标"，答案就已经在那里了。

### 问题四：你的 OS 跑在什么上？

RISC-V 64 + QEMU `virt` 是本课程的技术默认。但这不意味着你被绑死在 RISC-V 上。你的 ArchitectureSeed 中的 `target_platform` 字段可以声明任何 ISA——你只需要理解不同 ISA 之间的关键差异。

**三大 ISA 的关键差异，对你的 OS 设计的影响：**

| 维度 | RISC-V 64 | AArch64 (ARMv8) | x86-64 |
|------|-----------|-----------------|--------|
| 特权级 | M/S/U 三级，清晰分层 | EL3/EL2/EL1/EL0 四级 | Ring 0/1/2/3，历史包袱重 |
| 页表 | Sv39 (3级)，规范约100页 | VMSAv8-64 (4级)，规范复杂 | 4-level PML4，历史兼容多 |
| 中断控制器 | PLIC（平台级），简单清晰 | GICv3/v4，功能丰富但复杂 | APIC/x2APIC，最复杂 |
| syscall | `ecall` 指令，统一入口 | `svc` 指令，统一入口 | `syscall`/`sysenter`，历史遗留多 |
### 实现语言的选择

内核可以用多种系统编程语言编写。没有一种在所有维度上最优——你的选择取决于你最看重什么。

| 维度 | C | Rust (no_std) | Zig |
|------|---|---------------|-----|
| 内存安全 | 完全依赖程序员纪律。所有内存 bug 都是程序员的直接责任 | 编译期所有权+借用检查杜绝 use-after-free 等 bug 类别；`unsafe` 用于 MMIO/页表操作 | 编译期无所有权检查；提供 `defer`、错误联合类型、`@setRuntimeSafety` 等防御工具 |
| 代表项目 | Linux、xv6、FreeBSD、seL4、XNU——几乎所有工业内核 | Redox（微内核）、Tock（嵌入式RTOS）、rCore、Theseus（语言级隔离）| Bun（JS运行时）、TigerBeetle（金融DB）——裸机/系统编程正在增长 |
| 构建系统 | Make/CMake/Meson，需手动配置交叉编译 | Cargo + `rustup target add` 一行命令 | `build.zig` 可编程构建；`zig cc` 可作为 C 编译器 |
| 交叉编译 | 需手动安装目标工具链 | `rustup target add` | `zig build -Dtarget=riscv64-freestanding` |
| 学习曲线 | 语法简单（~32关键字），UB 陷阱隐蔽且多 | 所有权+借用+生命周期需数周适应，编译器错误信息质量高 | 语法中等，`comptime`（编译期代码执行）是独特优势 |
| 适合你的场景 | 想把精力全部花在 OS 设计上；参考资料最丰富 | 想用编译器消灭内存 bug；愿意投入时间学习所有权 | 想要一流交叉编译体验；对前沿工具链感兴趣 |

选择什么语言，在 ArchitectureSeed 的 `constraints` 字段中声明。无论选什么，内核都需要与硬件直接交互——这是所有语言的共同挑战。

### 问题五：用户程序怎么和你的内核交互？

**可执行格式。** ELF 是工业标准。它定义了一个可执行文件的结构——ELF header、Program Headers（描述哪些段加载到内存的什么位置）、Section Headers（用于调试和链接）。你的加载器需要知道怎么读 ELF。

**Syscall 触发。** RISC-V 用 `ecall` 指令。用户态执行 `ecall`，CPU 自动切换到 S-mode，跳转到 `stvec` 指向的地址。`a7` 寄存器存放 syscall 编号，`a0-a5` 存放参数，`a0` 承载返回值。这个机制是硬件定死的——你没有选择。

**Syscall 集合。** 这是你能选择的。你打算提供哪些 syscall？"类 POSIX 子集"是最常见的路线——`read/write/open/close/exit/fork/exec/wait`。但类 POSIX 不是唯一方案。你也可以设计一套更精简的 syscall——比如只有一个 `send(port, msg)` 和一个 `recv(port, msg)`——像微内核的做法。在阶段 1 你只需要定调，具体编号和参数留到阶段 7 再敲定。

## 1.5 ⚡ 挑战性设计问题

以下问题不是每个学生都需要回答的。它们面向有 OS 开发经验或追求更深设计理解的学生。如果你选择深入这些问题，在你的 ArchitectureSeed 的 ADR 中记录你的思考。

### 挑战 1：如果你选择微内核，IPC 性能如何保证？

微内核的致命弱点是 IPC 开销——每次服务调用都涉及上下文切换。在 seL4 中，一次 IPC 大约需要 150 个 CPU 周期（经过高度优化）。在你的初步实现中，一次 IPC 可能需要 2000+ 个周期。关键优化点：

- **零拷贝 IPC**：大消息通过共享内存页传递，只传递页的 capability 引用
- **syscall 批处理**：一次 `ecall` 提交多个操作，分摊上下文切换成本
- **快速路径**：非阻塞 IPC 走优化的快速路径；复杂的阻塞/超时走慢速路径

**你的 ArchitectureSeed 需要回答**：你选择微内核是因为它的隔离优势——你愿意为这个优势付出多大的 IPC 开销？

### 挑战 2：Exokernel 的 "libOS 边界" 怎么划？

Exokernel 只做资源隔离不做抽象。但"不做抽象"在实践中是什么含义？文件系统是 libOS 的事——但如果两个应用使用不同的 libOS 文件系统，它们的文件能共享吗？能，如果底层存储块被正确隔离和共享。但谁负责块的分配和访问控制？内核只管"应用 A 可以访问磁盘的第 1000-2000 块"——至于这些块怎么解释成"文件"，是两个 libOS 的事。

**关键张力**：内核越薄，libOS 之间的互操作性越差。内核提供的共享抽象越多（如 VFS），exokernel 的设计理念就越被稀释。

### 挑战 3：语言选择的深层影响

**Rust 内核中，`unsafe` 的边界在哪里？** 所有 MMIO 操作、页表操作、上下文切换都是 `unsafe`。但如果你的 `unsafe` 块太大（一个 `unsafe` 包裹了整个 `syscall` 函数），你实际上放弃了 Rust 的安全保证。如果你用 Rust 写内核，你的 ArchitectureSeed 应该声明 `unsafe` 的使用策略——每个 `unsafe` 块应最小化，并在文档注释中解释为什么编译器不能证明这段代码的安全性。

**Zig 内核中，`comptime` 如何改变构建系统？** Zig 允许你在编译期执行代码——这意味着你可以在编译期生成链接脚本、计算对齐、配置内存布局，而不是写脆弱的 Makefile 魔法。

### 挑战 4：阶段 8 的前瞻——你的 ArchitectureSeed 为方向组合预留了什么？

你现在做的设计决策会影响阶段 8 的方向选择空间：

- 如果你选了 fd-based 资源模型 → C1 (Linux ELF) 容易做，C4 (NT PE) 需要句柄转 fd 的翻译层
- 如果你选了 capability-based 模型 → C4/C5 需要将 NT handle 或 XNU fd 映射到 capability
- 如果你的调度器只支持单核 → 扩展到多核需要重新设计调度器（多核支持贯穿阶段 2-7，各阶段教学内容已包含对应的多核设计考量）
- 如果你的 HAL 层很薄 → H1 (USB) 和 H2 (PCI) 需要先重构 HAL

**要求**：在 ArchitectureSeed 中至少列出 2 个你预期在阶段 8 可能探索的方向，并说明你的当前设计如何为它们预留空间。

## 1.6 参考系统深度剖析——不只是"借鉴了什么"

ArchitectureSeed 要求你声明参考系统和借鉴/拒绝关系。但要做出有意义的借鉴和拒绝，你需要知道这些系统骨子里是怎么设计的。以下是四个最有教学价值的参考系统。

### Linux：宏内核的工业巅峰

Linux 是宏内核的标杆。核心设计哲学：**内核提供机制，用户态决定策略。**

关键机制：CFS/EEVDF 调度器——以虚拟运行时间和虚拟 deadline 组织公平调度。VFS（虚拟文件系统切换）——文件系统驱动只需实现操作结构中的函数指针，内核通过 VFS 层透明分发给任何文件系统。Buddy system 做物理页分配，slab 分配器做内核对象缓存。

对你的启示：借鉴 VFS 的"统一接口"思路。拒绝 Linux 调度器的复杂性——你的调度器不需要红黑树和虚拟 deadline。借鉴 slab 的思路——你的分配器可能需要区分"分配页"和"分配小于一页的内核对象"。

### seL4：微内核的形式化极致

seL4 是人类历史上第一个被完整形式化验证的通用操作系统内核。

关键机制：capability 空间——所有资源（内存页、线程、IPC 端点、中断）通过 capability 引用。capability 不可伪造（用户不能"猜"一个 capability）、可传递（通过 IPC 发送）、可派生（创建权限更窄的子 capability）、可撤销。内核约 9000 行 C，在 Isabelle/HOL 中被证明满足其形式化规范——包括不会死锁、不会空指针、不会缓冲区溢出。

对你的启示：借鉴 capability 作为资源引用——即使不做形式化验证，capability 让你对"谁可以访问什么"有极其清晰的语义。借鉴"最小化信任计算基"的思路——内核中哪些代码是"如果这出错了，整个系统就没救了"的？把这个集合尽可能缩小。

### XNU (macOS/iOS 内核)：混合内核的工程妥协

XNU 是 Mach（卡内基梅隆大学的微内核）+ BSD（Unix 的宏内核组件）的混合体。

关键机制：Mach 提供"任务"（地址空间 + 线程）和"端口"（IPC 通道）。BSD 层提供 POSIX syscall、VFS、网络栈。IOKit 连接两者。Mach 端口是 capability-like 的——发送者和接收者通过端口权限交互。但 XNU 把 BSD 层跑在内核态（不是用户态），所以它不是"纯微内核"。

对你的启示：借鉴分层思路——资源抽象层（类似 Mach ports）和兼容层（类似 BSD syscall）可以是两个独立模块。拒绝把整个 BSD 层塞进内核——教学 OS 探索微内核时，应该真把服务移到用户态。

### Windows NT：对象管理器的独特路径

Windows NT 的内核架构走了一条和 Unix 完全不同的路。

关键机制：对象管理器——所有资源（文件、进程、线程、事件、互斥量、注册表键）都是"对象"，统一在对象管理器命名空间中。每个对象有类型（决定支持什么操作）、安全描述符（ACL）、句柄（进程私有的引用）。`CreateFile` 表面是"创建文件"，实际通过对象管理器创建任何类型的对象。IRP（I/O Request Packet）是 I/O 的通用模型——驱动栈中每层可以处理、修改或传递它。

对你的启示：借鉴对象管理器统一命名的思路——"文件"和"事件"在同一个命名空间。拒绝 IRP 的复杂性——教学 OS 不需要驱动栈。借鉴 ACL 的安全模型——每个资源有自己的访问控制列表。

### 如何用这些参考系统

不要尝试"综合它们"——那会得到一个谁都不像的四不像。选择一个作为主要参考（借鉴它的核心架构决策），一个作为对比参考（"我在哪些方面故意和它不同"），其余的作为你的设计词汇。你的 ArchitectureSeed 中的 borrowed/modified/rejected 应该精确到具体机制层面——"借鉴 Linux 的 VFS 统一接口，拒绝 Linux 调度器的复杂性"，而不是"借鉴 Linux"这种模糊标签。

现代案例也一样。不要只写"参考 Rust for Linux"、"参考 CHERI"或"参考 Verus"。你要写清楚借鉴的是哪一个机制：Rust 的 `unsafe` 边界收敛、CHERI 的 capability 指针、Verus 的代码级不变量证明，还是 EEVDF 的虚拟 deadline。机制写清楚后，再写你改了什么、删了什么、用什么测试证明它确实进入了你的 OS。

以下是三个完整的 ArchitectureSeed 示例，展示了三种不同的设计立场：

**示例 A：教学优先的宏内核**

```yaml
goals:
  - "代码清晰性优先于性能——每个模块的核心函数不超过 50 行"
  - "学生可在 4 小时内理解全部内核代码（目标代码量 < 5000 行）"
  - "完整的自测体系——每个模块有不变量检查器"
non_goals:
  - "不追求 POSIX 兼容——设计自己的最小 syscall 集合"
  - "不支持多用户——所有程序以同一身份运行"
  - "不支持动态链接——所有程序静态链接"
  - "不支持网络"
reference_systems:
  - system: "xv6-riscv"
    borrowed_concepts:
      - "fork/exec/wait 进程模型"
      - "Sv39 分页的 identity mapping 方式"
    modified_concepts:
      - "syscall 接口从 21 个精简到 8 个"
    rejected_concepts:
      - "sleeplock——语义过于隐式"
      - "plic 复杂的初始化——教学场景中硬编码 PLIC 配置"
    reason: "精简到最核心的机制，让学生看到的是骨架而不是血肉"
```

**示例 B：兼容 Linux 静态 ELF 的宏内核**

```yaml
goals:
  - "能运行为 RISC-V Linux 编译的静态链接 ELF hello 程序"
  - "实现足够的 POSIX syscall 子集以运行 busybox（迷你 Shell）"
non_goals:
  - "不支持动态链接——仅静态 ELF"
  - "不支持多线程——每个进程单线程"
  - "不支持信号（signal）机制"
reference_systems:
  - system: "Linux (RISC-V)"
    borrowed_concepts:
      - "Linux syscall ABI——编号复用 Linux 的编号"
      - "ELF64 格式的完整 Program Header 解析"
    rejected_concepts:
      - "VMA（虚拟内存区域）管理——用更简单的固定布局"
      - "Linux 调度器的复杂性——用简单的轮转调度"
    reason: "兼容 Linux ABI 的好处是立即可运行已有程序，代价是继承了 ABI 的复杂度"
```

**示例 C：探索 capability 安全的微内核**

```yaml
goals:
  - "内核只提供地址空间、线程和 capability IPC"
  - "所有资源（文件、设备、内存）通过 capability 访问"
  - "capability 不可伪造、可传递、可撤销"
non_goals:
  - "不兼容任何已有 ABI——资源模型完全不同"
  - "不支持 POSIX 兼容层（阶段 8 可能做）"
reference_systems:
  - system: "seL4"
    borrowed_concepts:
      - "capability 作为不可伪造的访问令牌"
      - "capability 空间由内核管理"
    rejected_concepts:
      - "seL4 的形式化验证——教学 OS 不需要走到这个深度"
    reason: "capability 模型提供了最清晰的'谁可以访问什么'的语义"
```

## 1.7 动手：你的 ArchitectureSeed 第一稿

现在写。别继续往下读了。打开编辑器，创建 `spec/architecture/seed.yaml`。把这个骨架填上你的内容：

```yaml
goals: []           # 至少 3 条
non_goals: []       # 至少 3 条——"不做什么"和"做什么"一样重要
constraints: []     # ISA？运行环境？内存上限？
reference_systems:
  - system: ""         # 你参考的系统名
    borrowed_concepts: []
    modified_concepts: []
    rejected_concepts: []   # 至少 1 条
    reason: ""              # 拒绝的理由
```

写完第一稿，自查这几件事：

- rejected_concepts 有没有附带 reason？没有理由的拒绝不是设计判断，是选口味。
- non_goals 有没有至少 3 条？只有 1 条的 non_goals 说明你可能还没认真想过边界的范围。
- initial_validation_binding 能不能测？"系统稳定运行"不能测。"串口输出 kernel banner，QEMU 在 30 秒内无 panic"能测。"用户体验良好"不能。"用户程序 hello 可在 QEMU 启动后 3 秒内输出"能。

跑 `vos arch lint` 检查格式完整性。

你有整个阶段 1 的时间来迭代这份 ArchitectureSeed。后续阶段你会发现它的某些部分需要修改——用 SpecPatch 记录，别偷偷改了假装没发生过。

## 1.8 常见陷阱

**目标过大。**"我要做一个比 Linux 更好的 OS"——这句话在 ArchitectureSeed 里不能出现。不是因为不对，是因为不可操作。把目标缩小到你能在一学期内验证的程度。

**参考系统写成标签。**"参考 Linux"这句话提供了零信息。Linux 有调度器、VFS、网络栈、内存管理、几百个 syscall——你具体借鉴了哪个子系统、哪个机制？精确到机制层面的借鉴才有设计指导价值。

**拒绝理由空洞。**"拒绝 xv6 的 XXX 因为太复杂"——为什么复杂？是数据结构复杂？是并发控制复杂？还是接口复杂？说清楚复杂度在哪，你的拒绝才有说服力。

**忘了写 non-goals。**只写"我要做 X"不写"我不做 Y"，会在阶段 5 的时候让你在"要不要实现网络栈"这个问题上纠结两天。现在花十分钟写清楚 non-goals。

**验证判据像广告语。**"高度安全"不可测。"任意用户程序无法访问内核物理页"可测。把抽象判断落回具体、可观测的指标。

## 1.9 本章小结

你产出了一份 ArchitectureSeed。它应该诚实——你的目标、你的约束、你的借鉴和拒绝，原样写在那里。

这份文件是你后续八个阶段的设计锚点。任何后续决策如果和它冲突，你要么修改决策，要么通过 SpecPatch 诚实地记录演化——不能偷偷改了假装一直如此。

进入阶段 2 之前，你能对另一个同学讲清楚吗——为什么你选了宏内核？为什么拒绝了参考系统中的那个概念？你的 OS 最大的约束是什么？

能讲清楚，你的设计是设计。讲不清楚，你的设计是碰巧。

下一章：[第 2 章：最小内核启动](ch02-boot.md)
