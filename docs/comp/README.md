# Historical competition documents

The PDF/TeX/Markdown files in this directory are historical proposal artifacts and are not the active student contract. Use the repository README and `docs/design/{spec,toolchain,workflow,agent}` for the v2 implementation.
