spec_root = "spec"

[agent]
provider = "gemini"
model = "gemini-3.5-flash"
base_url = "https://generativelanguage.googleapis.com"
timeout_secs = 600
[agent.auth]
env = "GEMINI_API_KEY"

[agent.retry]
max_attempts = 2
