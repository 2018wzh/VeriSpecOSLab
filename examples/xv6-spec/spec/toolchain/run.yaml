# xv6 RISC-V QEMU run specification
# Extracted from build-phases.yaml and toolchain.yaml run sections.

run:
  emulator: qemu-system-riscv64
  machine: virt
  cpu: rv64
  memory: 128M
  bios: default
  kernel_arg: build/kernel.elf boot-smoke
  success_signal: XV6_BOOT_OK
  timeout_secs: 30
